#pragma once
#include <string>
#include <string>

using namespace std;

static class PalindromeChecker
{
	public:
		static bool isPalindrome(string, int, int);
		static void menu();
		static string getString();
};

